import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gentledragonrest',
  appName: 'gentle-dragon-rest',
  webDir: 'dist'
};

export default config;
